import React from 'react';
import ReactDOM from 'react-dom/client';
import QloLandingPage from './QloLandingPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QloLandingPage />
  </React.StrictMode>,
);